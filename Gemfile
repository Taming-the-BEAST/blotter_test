source 'https://rubygems.org'

# jekyll
gem "jekyll"

# minifying
gem 'jekyll-press'

# references
gem 'jekyll-scholar'

# octokit
gem 'octokit'
gem 'netrc'
gem 'faraday-retry'

# didn't compile
gem 'rouge'
gem 'listen'

gem 'jekyll-katex'

# stdlib gems no longer bundled by default in Ruby >= 3.5 (needed by jekyll)
gem 'csv'
gem 'base64'
gem 'ostruct'
gem 'observer'
